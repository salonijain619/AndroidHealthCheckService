# ICM Team 106961 Live Pull — 2026-06-08

- **Source:** `agency mcp icm` (HarryPotter-pattern collector, ported verbatim — same code as 2026-06-06 pull, no changes)
- **Team:** GSA Client - Android (`106961`)
- **Run timestamp UTC:** `2026-06-08T12:01:45.108073+00:00` (per server `_meta.fetched_at`)
- **Local invoke:** `python3 -m tools.icm.icm_collector --config .squad/config.json --timeout 120 > tools/icm/runs/icm-run-2026-06-08.json`
- **Elapsed:** ~30s (warm Entra cache, no browser flow)
- **Raw JSON drop:** `/Users/salonijain/workspace/AndroidHealthCheckService/tools/icm/runs/icm-run-2026-06-08.json` (5,335 bytes — byte-identical size to 2026-06-06)
- **`source`:** `live` · **`_meta.errors`:** `[]` · **warning:** none
- **stderr:** empty (`tools/icm/runs/icm-run-2026-06-08.stderr` is 0 bytes)

---

## 📟 On-Call

- **🔴 Primary:** dileepkusuma (`dileepkusuma`)
- **🟡 Backup:** samirnen (`samirnen`)
- **Schedule source:** `get_on_call_schedule_by_team_id` → `live` (no cache fallback)
- **Δ vs 2026-06-06:** **no rotation change** — same primary, same backup.

---

## 🚨 Active ICM Incidents — Counts

- **Total Active+Mitigating:** **1** (unchanged from 2026-06-06)
- **By severity:** Sev0–2: 0 · **Sev3: 1** · Sev4: 0
- **Customer-reported (`type == "CustomerReported"`):** **1**
- **System-created (LiveSite / Deployment / detector):** **0**
- **TestICM-flagged:** **1** (the lone active ICM, `keywords` = "[IcM Copilot] Test incident / no validated production issue")
- **Effective real-incident count (excluding TestICM):** **0**

> ⚠️ **CLASSIFICATION GUIDANCE FOR REYES — READ THIS:**
> The collector still has the known bucketing bug (filed 2026-06-06): it splits active ICMs by `source startswith "customer"`, but ICMProd's payload uses the `type` field (returns `CustomerReported` / `LiveSite` / `Deployment`) and `source` is `None`. As-shipped, the customer-reported ICM lands in `system_created_active` in the JSON envelope.
> **Trust the `type` field, NOT `source`, NOT the `customer_created_active` / `system_created_active` arrays.** The `active_icms` array is correct; classify each entry by `type == "CustomerReported"` for the report's "Customer-Created" table. (Confirmed today: `customer_created_active = []` and `system_created_active = [810723164]`, but `active_icms[0].type == "CustomerReported"` — same bug, same workaround as 06-06.)

---

## 👤 Customer-Reported Active (classified by `type`, not `source`)

| ICM ID | Sev | Age | Title | Status | Cust-Reported | TestICM |
|---|---|---|---|---|---|---|
| [810723164](https://portal.microsofticm.com/imp/v3/incidents/details/810723164/home) | 🟡 Sev3 🧪 | **5d** (created 2026-06-03T07:32Z, last-modified 2026-06-06T06:01Z) | `[Copilto testing] GSA Android client not working` | ACTIVE (**unacknowledged**) | yes (`type=CustomerReported`, `isCustomerImpacting=true`) | **yes** (keywords flag) |

- **Owning team (verbatim):** `"GSA  Client - XPlat"` ← **double-space typo confirmed again today** (between "GSA" and "Client"). `owningTeamId=106961`. The drift Saloni asked about on 2026-06-06 has not been corrected upstream.
- **Assigned to:** `assignedTo=422691` (numeric — collector does not resolve to alias)
- **Last-modified:** `2026-06-06T06:01:04.97Z` — **NO movement on the ICM since the prior pull** (i.e., nobody has touched it for ~2.5 days; still unacknowledged 5 days in).

## 🤖 System-Created Active

| ICM ID | Sev | Age | Title | Status |
|---|---|---|---|---|
| — | — | — | _No system-detected (LiveSite / Deployment / detector-emitted) ICMs on team 106961_ | — |

## 🟡 Mitigated Highlights (last 7d window — empty)

| ICM ID | Sev | Age | Title | Status |
|---|---|---|---|---|
| — | — | — | _Zero mitigated ICMs returned for `owningTeamId=106961`_ | — |

> Empty result is a **real signal**, not a query-shape bug — `search_incidents(states=[Mitigated], sortBy=LastModifiedDate Desc, top=50, NO dateRange)` D-138 discipline preserved. Either nothing has been mitigated under this team in queue history, or mitigated work is filed under the parent `GSA Client - XPlat` umbrella (consistent with the owning-team-name drift). Same observation as 2026-06-06.

---

## Δ vs 2026-06-06 pull

| Dimension | 2026-06-06 | 2026-06-08 | Δ |
|---|---|---|---|
| Active ICMs (count) | 1 | 1 | unchanged |
| The active ICM (id) | 810723164 | 810723164 | **same incident, no movement** |
| Active ICM age | 3d | 5d | aged +2d, still unacked |
| Active ICM `lastModifiedDate` | 2026-06-06T06:01:04.97Z | 2026-06-06T06:01:04.97Z | **identical — nobody has touched it** |
| Mitigated (last 7d) | 0 | 0 | unchanged |
| New ICMs since last pull | — | **0** | quiet |
| Mitigated since last pull | — | **0** | quiet |
| On-call primary | dileepkusuma | dileepkusuma | unchanged |
| On-call backup | samirnen | samirnen | unchanged |
| `oncall.schedule_source` | live | live | unchanged |
| Owning-team name | `GSA  Client - XPlat` | `GSA  Client - XPlat` | double-space typo persists |
| Effective real-incident count | 0 | **0** | unchanged |
| `_meta.errors` | `[]` | `[]` | clean |

**Two pulls in a row, byte-identical envelope size (5,335 bytes), zero state change.** The queue is genuinely quiet *and* the lone test ICM has gone untouched for 2.5 days. Nothing has been newly opened, mitigated, or escalated.

---

## Patterns (Scully's read — Skinner / Reyes to refine)

- **Dead-quiet queue (2nd consecutive pull).** Same Sev3 TestICM, no new activity, no mitigations, on-call unchanged. Effective real-incident count = 0 today and 06-06.
- **Aging-stale TestICM.** `[Copilto testing]…` (#810723164) is **5 days old, unacknowledged, last-touched 2026-06-06T06:01Z**. Identical lastModified across both our pulls. Recommend on-call (`dileepkusuma`) close or mark non-actionable so it stops eating "active count" optics. Filed 06-06; still open 06-08.
- **Detector silence persists, and now also covers Reyes's v1 anchor finding.** The 2026-06-05 server-side pull found a 5× tunnel-failure-rate ramp (0.074%→0.36% sustained) and a 100%-fail `PROFILE_UNDEFINED` cohort. Three days later, **zero detector-emitted ICMs** on team 106961 reflect that. Either no Android detectors are wired in, or the routing is misdirecting them. Suspicious, not reassuring. (Mulder follow-up was already filed; this pull is a 2nd data point.)
- **Owning-team double-space typo (`"GSA  Client - XPlat"`) persists** — not corrected upstream in 2.5 days. Cosmetic but confirms the queue identity is XPlat-parented (vs Android-only sub-queue). Open question on whether 106961 is the right scope for Android or if there's a child queue we're missing.
- **Mitigated history continues to be empty** — `top=50` D-138-clean Mitigated search still returns 0. Either team 106961 has never had a mitigated ICM (improbable for a multi-month-old queue) OR mitigated work is filed under the parent `GSA Client - XPlat` umbrella. Worth one cross-team probe (manual portal lookup against `owningTeamId` aliases) when Saloni has 2 minutes.
- **Bucketing bug still ships** — `customer_created_active=[]` while the lone real customer-reported ICM is in `system_created_active`. Doggett owns the v2.1 fix (one-line change to switch the predicate from `source startswith "customer"` to `type == "CustomerReported"`). Until then, downstream consumers MUST classify on `type`. Restating this here so the bug is documented in two consecutive pulls and Reyes is unambiguous on what to render.

---

## Raw counts for cross-check

- `search_incidents(states=[Active, Mitigating], top=50, NO dateRange)`.count = **1**
- `search_incidents(states=[Mitigated], sortBy=LastModifiedDate Desc, top=50, NO dateRange)`.count = **0**
- `get_on_call_schedule_by_team_id(teamId=106961)` → 2 contacts (primary + backup)
- `get_ai_summary(incidentId=810723164)` → fired (Sev3 active passed the gate); summary stored in `ai_summaries["810723164"]`. Title from AI: "Global Secure Access Android Client Access Failure". Severity / state confirm Sev3 / ACTIVE; no root cause / no mitigation in AI text.

## Provenance / D-138 verification

D-138 discipline preserved this run — neither `search_incidents` envelope carries a `dateRange` key. Test suite still 19/19 (not re-run today; no code changes since 06-06). `team_id=106961` resolved from `.squad/config.json::icm.team_id` (no `--team-id` CLI override, no `AHCS_ICM_TEAM_ID` env). Skill `.squad/skills/icm-queue-ingest/SKILL.md` followed verbatim — 5-step handshake including the load-bearing 6s warmup; no deviations.
