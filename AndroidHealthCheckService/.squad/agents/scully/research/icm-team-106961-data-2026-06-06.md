# ICM Team 106961 Live Pull — 2026-06-06

- **Source:** `agency mcp icm` (HarryPotter-pattern collector, ported verbatim)
- **Team:** GSA Client - Android (`106961`)
- **Run timestamp UTC:** `2026-06-08T10:07:00.811859+00:00` _(per server `fetched_at`; collector was invoked under the squad's 2026-06-06 work-day clock — note ~2-day skew between requesting-clock and ICM-server clock; downstream age calc uses `fetched_at`)_
- **Collector version:** ported from HP commit `d35a114` (`feat(livesite): collectors, template, AppCenter, tests for D-138→D-148`)
- **Elapsed:** ~26s (cold-cache Entra token still warm from Saloni's prior `agency mcp icm` session — no browser flow this run)
- **Raw JSON drop:** `/Users/salonijain/workspace/AndroidHealthCheckService/tools/icm/runs/icm-run-2026-06-06.json` (5,335 bytes)
- **`source`:** `live` · **`_meta.errors`:** `[]` · **warning:** none

---

## 📟 On-Call
- **🔴 Primary:** dileepkusuma (`dileepkusuma`)
- **🟡 Backup:**  samirnen (`samirnen`)
- **Schedule source:** `get_on_call_schedule_by_team_id` → `live` (single shift block, two contacts; no rotation oddities surfaced)

> ⚠️ Display names came back identical to aliases — ICM directory likely not resolving `displayName` for this team. Cosmetic only.

---

## 🚨 Active ICM Incidents — Counts

- **Total Active+Mitigating:** **1**
- **By severity:** Sev1: 0 · Sev2: 0 · **Sev3: 1** · Sev4: 0
- **Customer-created (`type=CustomerReported`):** 1
- **System-created:** 0
- **TestICM flagged:** **1** (titled `[Copilto testing]…`; `keywords` says "Test incident / no validated production issue")

> ⚠️ **Routing/bucketing note for Reyes:** the ported collector buckets via `source startswith "customer"` (HP convention). ICMProd's actual payload uses `type` (`CustomerReported` / `LiveSite` / `Deployment` / …) and returns `source: None` on this queue. As-shipped, this incident lands in the **`system_created_active`** array even though it is genuinely customer-reported. Flagging for a v2.1 follow-up. The raw `type` field is preserved in the per-incident dict so a downstream re-bucket is one line. (Filed as a Doggett/Reyes touch-up — not a blocker.)

---

## 👤 Customer-Created Active

| ICM ID | Sev | Age | Title | Status |
|---|---|---|---|---|
| [810723164](https://portal.microsofticm.com/imp/v3/incidents/details/810723164/home) | 🟡 Sev3 🧪 TestICM | 5d (created 2026-06-03) | `[Copilto testing] GSA Android client not working` | ACTIVE (unacknowledged) |

_Note: per the bucketing caveat above, this row currently flows through `system_created_active` in the raw envelope. Listed here because `type=CustomerReported`._

## 🤖 System-Created Active

| ICM ID | Sev | Age | Title | Status |
|---|---|---|---|---|
| — | — | — | _No system-detected active ICMs_ | — |

## 🟡 Mitigated Highlights (top by LastModifiedDate, no `dateRange` per D-138)

| ICM ID | Sev | Age | Title | Status |
|---|---|---|---|---|
| — | — | — | _No mitigated ICMs returned for team 106961_ | — |

> Empty result is genuine: `search_incidents(states=[Mitigated], sortBy=LastModifiedDate Desc, top=50)` returned 0 items. Either team 106961's mitigation history is genuinely empty under the `owningTeamId` filter, or mitigated work historically lands under the **`GSA Client - XPlat`** umbrella (the active incident's `owningTeamName` came back as `GSA  Client - XPlat`, despite `owningTeamId=106961` — a naming/team-display drift worth confirming with Saloni). D-138 discipline is preserved — the empty result is not a windowing artifact.

---

## Patterns (Scully's preliminary read — Skinner/Reyes to refine)

- **Quiet week on team 106961.** 1 active ICM total, Sev3, customer-reported, but **flagged as a test incident** by the reporter (`keywords: "Test incident / no validated production issue"`). Effective real-incident count: **0**.
- **Zero system-detected ICMs.** No LiveSite / Deployment / detector-emitted incidents on the queue. Cross-check against Scully's NaaS 7d telemetry pull (2026-06-05): server-side **0.36% sustained tunnel failure rate** (~5× ramp over the window) and `PROFILE_UNDEFINED` 100%-fail cohort would normally be detector-bait — **detector silence is suspicious**, not reassuring. Suggest Mulder ack and Skinner trace whether any Android-tagged detector exists at all.
- **Unacknowledged Sev3 aging 5 days.** `isAcknowledged: false` on the only active item. Even for a [TestICM], no ack from on-call in 5 days suggests rotation isn't watching this queue closely — primary `dileepkusuma` should triage today (or the TestICM should be cleaned up by the reporter).
- **TestICM saturation = 100%** of active inventory. Recommend `[Copilto testing]…` (#810723164) be closed/cleaned-up before it skews next cycle's "active count" optics.
- **Owning-team drift:** `owningTeamId=106961` returns `owningTeamName="GSA  Client - XPlat"` (note the double-space typo in ICM), not "GSA Client - Android". Worth a one-line confirm with Saloni that **106961 is the right queue** (vs an XPlat parent vs an Android-specific child). If 106961 is the right Android queue and just labeled "XPlat", we're fine; if there's a separate Android-only sub-queue, we may be missing scope.
- **On-call rotation looks healthy** — two distinct contacts, single active shift, no warning flags, no cache fallback.

---

## Raw counts for cross-check

- `search_incidents(states=[Active, Mitigating], top=50, NO dateRange)`.count = **1**
- `search_incidents(states=[Mitigated], sortBy=LastModifiedDate Desc, top=50, NO dateRange)`.count = **0**
- `get_on_call_schedule_by_team_id(teamIds=[106961])` → **2 contacts** returned (primary + backup, 1 shift block)
- `get_ai_summary(incidentId=810723164)` → fired (Sev3 ≤ collector's gate of "Active/Mitigating OR sev ≤ 2"; Sev3 active passed the state check). AI summary length: stored in `ai_summaries["810723164"]`.

## Provenance / D-138 verification

The two `search_incidents` request envelopes captured this run (re-derivable from the 19 passing unit tests, including the 3 D-138 regression tests) carry **no `dateRange` key** and the Mitigated call carries `sortBy=[{"field":"LastModifiedDate","direction":"Descending"}]`. The empty `mitigated_icms` array is therefore a **real signal** about queue 106961, not a query-shape bug. Same D-117/D-118 discipline as HP: `team_id=106961` lives in `.squad/config.json::icm.team_id` only; CLI flag `--team-id` and env `AHCS_ICM_TEAM_ID` override.
