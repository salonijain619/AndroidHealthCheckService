# Orchestration: scully — 2026-06-05T12:40:00Z

**Agent:** scully (Telemetry Analyst)
**Type:** general-purpose, background
**Model:** claude-opus-4.7
**Spawned by:** coordinator

## Task
Ingest the `gsa-kusto-catalog` plugin from the locally cloned `Identity-gsa-client-marketplace/` marketplace and reconcile against the squad's four standing routing unknowns.

## Outcome
- **CLOSED 3 of 4 standing unknowns:**
  1. PKI source → `naas-idsharedwus / NaasCloudPkiProd / EnrollCertificateOperationSummary` (time col `PreciseTimeStamp`).
  2. App Insights component → `wd-prod-android-client` (sub `fb633419-…`); `AndroidId` in `customDimensions`, version in `application_Version`. App Insights REST API, NOT a Kusto cluster URL.
  3. Android client telemetry pipeline → **App Insights**, NOT Aria. Prior charter assumption (Aria `mnap_xplat_*` as Android-primary) is **wrong**; those tables are Win/Mac primary. Android appears in Aria only opportunistically (e.g., `errorevent` via `App_Platform == 'Android'`).
- **NEW discoveries:**
  - `naas-idsharedwus / NaasProd` is a **2-table mirror** (Tunnel + Edge). The full 37-table `NaasProd` lives on `naas-idsharedscus` (`https://idsharedscus.southcentralus.kusto.windows.net`). Cross-checks against Roxy / Talon / ControlTower / NaaSVPN* / CertMonitor require SCUS routing.
  - New cluster: `androidgsa.eastus.kusto.windows.net / Metric` — Android perf rollups (`MemoryCPUUsage`, `UploadDownloadSpeed`) by AppVersion + day. Catalog-flagged as not live-verified (DNS failed during catalog generation).

## Artifacts written
- `.squad/skills/gsa-kusto-catalog-android-slice/SKILL.md` (NEW, confidence: MEDIUM — derived from upstream, not independently re-verified)
- `.squad/skills/android-kusto-starter/SKILL.md` (UPDATED — bumped query confidences, added queries #8 PKI, #9 App Insights, #10 perf rollups, #11 Aria errorevent cross-check)
- `.squad/agents/scully/research/dashboard-analysis.md` (UPDATED — added "Catalog Confirmation" section)
- `.squad/decisions/inbox/scully-kusto-catalog-adopted.md` (filed, now merged)

## Notes for next spawn
- Decision is PROPOSED, pending Mulder ack.
- Aria `database` parameter is the GUID (`f0eaa94222894be599b7cd0bc1e2ed6f`), never the friendly name — top documented failure mode upstream.
- Time-column inconsistency across the family: `TIMESTAMP` vs `PreciseTimeStamp` vs `env_time` vs `EventInfo_Time` vs `timestamp` vs `ingestion_time()`. Wrong column silently returns zero rows.
